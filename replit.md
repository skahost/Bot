# Pterodactyl Discord Bot

A production-ready Discord bot for managing a Pterodactyl Panel via its Application and Client APIs. Built with Discord.js v14 and TypeScript.

## Stack
- **Language**: TypeScript (Node.js)
- **Framework**: discord.js v14
- **Build tool**: esbuild (via `build.mjs`)
- **HTTP client**: undici

## Running the bot
```bash
npm install
npm run build
npm start
```

The configured Replit workflow (`Discord Bot`) runs `npm run build && npm start` automatically.

## Required secrets / environment variables

| Variable | Description |
|---|---|
| `DISCORD_TOKEN` | Discord bot token |
| `DISCORD_CLIENT_ID` | Discord application client ID (for slash command registration) |
| `DISCORD_GUILD_ID` | Optional. Guild ID for instant command registration (omit for global) |
| `PTERODACTYL_PANEL_URL` | Panel base URL, e.g. `https://panel.example.com` |
| `PTERODACTYL_API_KEY` | Application API key (Admin > Application API) |
| `PTERODACTYL_CLIENT_API_KEY` | Client API key (Account > API Credentials) |
| `OWNER_IDS` | Optional. Comma-separated Discord user IDs treated as admins |
| `LICENSE_KEY` | Required. Bot refuses to start without a valid key |

Copy `.env.example` to `.env` and fill in values, or set them as Replit secrets.

## Project structure
- `src/index.ts` — entry point
- `src/client.ts` — Discord client setup
- `src/commands/` — slash command implementations
- `src/handlers/` — interaction, message, and connection event handlers
- `src/services/` — Pterodactyl API, command registration, licensing logic
- `dist/` — compiled output (generated by build)

## User preferences
<!-- Add any personal preferences or conventions here -->
